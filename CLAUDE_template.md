# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

<!--
【このテンプレートの使い方】
- {{...}} のプレースホルダーを実際の内容に置き換える
- 各セクションの <!-- 記入指針 --> を読んで埋めた後、コメントごと削除する
- 当たり前のことは書かない（例:「エラーハンドリングを丁寧に」「テストを書く」等）
- 書く価値があるのは「読まなければ分からないこと」だけ
-->

## プロジェクト概要

<!-- 記入指針:
  - このシステムが「何を解決するか」を1〜2文で
  - 主なユーザーと利用文脈
  - スコープ外（やらないこと）を明示すると判断ミスが激減する
-->

**目的:** {{このシステムが解決する問題・提供する価値}}

**ユーザー:** {{主なユーザー像と利用場面}}

**スコープ外:** {{このシステムが意図的に対応しないこと}}

---

## 開発コマンド

<!-- 記入指針:
  - コマンドは実際に動くものだけ書く
  - 「テストを1件だけ実行する方法」は必須（Claude がデバッグ時に頻用する）
  - 環境構築手順が複雑な場合のみ記載。単純な npm install は不要
-->

```bash
# 開発サーバー起動
{{dev command}}

# ビルド
{{build command}}

# 全テスト実行
{{test command}}

# 単一テスト実行（ファイル指定 / テスト名指定）
{{single test command}}
# 例: npx vitest run src/hooks/useTodos.test.ts
# 例: npx jest --testNamePattern="should add todo"

# Lint / 型チェック
{{lint command}}
{{typecheck command}}
```

---

## アーキテクチャ

<!-- 記入指針:
  - 複数ファイルを読まないと分からない「全体像」を書く
  - データの流れ（入力→処理→出力）を明示する
  - ファイル一覧や単純なディレクトリ構造は書かない（Claude が自分で読める）
  - 「なぜこの設計か」の理由を添えると判断精度が上がる
-->

### システム全体図

```
{{入力元}} → {{処理層}} → {{出力先}}
例: クライアント → API Routes → Service層 → Repository層 → DB
```

### 主要モジュールの責務

| モジュール | 責務 | 備考 |
|-----------|------|------|
| {{module}} | {{責務}} | {{注意点・理由}} |

### データフロー

<!-- 状態がどこで生まれ、どこで変わり、どこで使われるかを書く -->
{{データフローの説明}}

### 重要な設計判断

<!-- 「なぜこうなっているか」を記録する。コードを見ても分からない背景を書く -->
- **{{判断}}: {{理由}}**
  例: Repository層でトランザクション境界を管理 → Service層のテストをDBなしで書けるため

---

## 技術スタック

<!-- 記入指針:
  - バージョンを明記する（動作が変わることがあるため）
  - 「なぜ採用したか」より「どう使っているか」のクセを書く
  - 採用しなかった選択肢の理由は書かない（判断の邪魔になる）
-->

| 技術 | バージョン | 用途・注意点 |
|------|-----------|-------------|
| {{技術名}} | {{version}} | {{このプロジェクト固有の使い方・制約}} |

---

## コーディング規約

<!-- 記入指針:
  - プロジェクト固有のルールのみ書く（一般的なベストプラクティスは不要）
  - 「なぜそのルールか」の背景も1行で添える
  - linter/formatter が自動で担保するルールは書かない
-->

### 命名

```
{{命名規則の例}}
例:
- イベントハンドラは on + 動詞: onSubmit, onDelete（Reactの慣習に合わせる）
- 副作用のある関数は動詞で始める: fetchUser, saveOrder（純粋関数と区別）
- 型名の接尾辞: Props / State / Params / Response（役割が一目でわかるため）
```

### ファイル・モジュール構成

```
{{モジュール分割のルール}}
例:
- 1ファイル1コンポーネント（テストで mock しやすくするため）
- index.ts による再エクスポートは禁止（循環参照の原因になるため）
```

### 状態管理

```
{{状態をどこに置くか・どう変更するかのルール}}
例:
- サーバー状態は React Query、UI状態は useState（混在させない）
- グローバル状態は Zustand store に集約（Context は prop drilling 回避のみに使用）
```

### エラー処理

```
{{エラーをどこでキャッチし・どこでユーザーに見せるか}}
例:
- API エラーは service 層でキャッチ → Result 型にラップして返す
- 予期しないエラーは Error Boundary でキャッチ → /error ページへ
```

---

## 重要な制約・不変条件

<!-- 記入指針:
  - 「絶対に破ってはいけないルール」のみ書く
  - 過去に壊れてインシデントになった経験則が最も価値が高い
  - 理由を必ず添える（理由がないルールは守られない）
-->

- **{{制約}}**
  理由: {{なぜこの制約が存在するか・破ると何が起きるか}}

```
例:
- DB マイグレーションは必ず後方互換にする
  理由: デプロイはローリング更新のため、旧コードと新スキーマが一時的に共存する

- 外部 API 呼び出しは必ず src/infrastructure/ 以下に閉じ込める
  理由: モック差し替えを一箇所にするため・テスト時の副作用を防ぐため

- ユーザー入力は必ず受け取った最初の層でバリデーションする
  理由: 内部関数まで不正値が伝播するとデバッグが困難になるため
```

---

## テスト戦略

<!-- 記入指針:
  - 「何をどのレイヤーでテストするか」の方針を書く
  - 「モックしてよいものとしてはいけないもの」を明示する
  - テストが壊れやすいパターン（過去の失敗）があれば記録する
-->

### テスト構成

```
{{テストの種類と対象}}
例:
- Unit: ビジネスロジック（Service 層）→ DB はモック
- Integration: API Routes → 実 DB（テスト用 Docker）
- E2E: クリティカルパスのみ（ログイン・購入フロー）
```

### モック方針

```
{{モックしてよいもの・してはいけないもの}}
例:
- 外部 API・メール送信: モック可
- DB: Integration テスト以外ではモック可
- 日時（Date.now）: モック必須（テストが時刻依存にならないため）
- ファイルシステム: モック可
```

### 注意事項

```
{{テスト固有の落とし穴}}
例:
- beforeEach で必ず DB をリセットする（テスト間の状態汚染を防ぐ）
- スナップショットテストは使用禁止（変更のたびに壊れるため）
```

---

## 既知の落とし穴・注意事項

<!-- 記入指針:
  - 「コードを読んだだけでは気づかない」非自明な挙動を書く
  - 過去に詰まったこと・バグになったことが最も価値が高い
  - ライブラリの挙動の癖、環境依存の問題なども含める
-->

```
例:
- {{ライブラリ名}} の {{メソッド}} は {{条件}} のとき {{予期しない挙動}} をする
  → 対処: {{回避策}}

- {{環境}} では {{処理}} が {{原因}} により遅い / 動かない
  → 対処: {{回避策}}

- {{API}} のレートリミットは {{制限値}}。バッチ処理時は必ず {{対策}} を入れる
```

---

## 外部依存・連携サービス

<!-- 記入指針:
  - 接続先・認証方法・エンドポイントの場所（ファイルパス or 環境変数名）
  - SLA・レートリミットなどの運用上の制約
  - ローカル開発時のセットアップ方法（モック or 実環境）
-->

| サービス | 用途 | 設定場所 | ローカル開発 |
|---------|------|---------|-------------|
| {{サービス名}} | {{用途}} | {{環境変数名 / 設定ファイルパス}} | {{モック方法 or 実環境接続手順}} |

---

## セキュリティ上の注意

<!-- 記入指針:
  - このシステム固有のセキュリティ要件のみ書く（一般論は不要）
  - 「どのデータが機密か」「どの処理が認可チェックを必要とするか」を明示する
-->

```
例:
- {{エンドポイント群}} は {{ロール}} のみアクセス可。ミドルウェア {{ファイルパス}} で担保している
- {{フィールド名}} はレスポンスに含めてはいけない（{{理由}}）
- {{操作}} を行う前に必ず {{チェック}} を実施する
```

---

## 参照リソース

<!-- 記入指針:
  - 設計判断の根拠になったドキュメント・ADR・仕様書へのリンク
  - 社内 Wiki・チケット管理ツールの関連プロジェクトへのリンク
  - 「なぜここに書いてあるか」が伝わる一言を添える
-->

- [{{ドキュメント名}}]({{URL}}) — {{なぜ参照すべきか}}
